name: Debug Info

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show gradle.properties
        run: cat gradle.properties

      - name: Show Arc hash and branch
        run: |
          grep -i "arc" gradle.properties
